# aq

`aq` (Any Query) 是一个致力于简化多格式配置文件（TOML, JSON, YAML 等）解析与查询的命令行工具。

它的设计目标是提供统一、简单的语法来处理不同格式的数据，类似于 `jq`，但支持更多格式，并且针对大文件进行了内存优化。

## ✨ 特性 (Features)

- **多格式支持**：
  - [x] **TOML**: 支持标准 TOML 文件解析与查询。
  - [ ] **JSON**: (计划中) 支持 JSON 解析。
  - [ ] **YAML**: (计划中) 支持 YAML 解析。
- **统一查询语法**：使用简单的点分路径（如 `server.port`）提取深层数据。
- **高性能**：
  - 针对大文件（Large Files）采用流式读取（Stream Decoder），极大降低内存占用。
- **格式化输出**：查询结果默认以格式化的 JSON 输出，方便阅读或配合其他工具使用。

## 🚀 安装 (Installation)

```bash
# 从源码安装
go install github.com/dzjyyds666/aq@latest
```

## 📖 使用指南 (Usage)

### TOML 解析

`aq` 的 `toml` 子命令用于处理 TOML 文件。

#### 1. 基础查询
读取文件并查找特定字段：

```bash
# 查找 config.toml 中 server 下的 port 值
aq toml -i config.toml -f server.port

# 查找 database 下的所有配置
aq toml -i config.toml -f database
```

#### 2. 全量转换
如果不指定 `-f/--find` 参数，`aq` 会将整个 TOML 文件转换为 JSON 输出：

```bash
aq toml -i config.toml
```

#### 3. 结果输出到文件
使用 `-o/--output` 参数将结果保存到文件：

```bash
aq toml -i config.toml -f database -o db_config.json
```

## 🗺️ 路线图 (Roadmap)

- [x] **v0.1**: 基础框架搭建，支持 TOML 解析与查询。
- [ ] **v0.2**: 支持 JSON 文件解析。
- [ ] **v0.3**: 支持 YAML 文件解析。
- [ ] **v0.4**: 支持文件格式互转 (e.g., TOML -> YAML)。

## 🤝 贡献 (Contributing)

欢迎提交 Issue 或 Pull Request！

## 📄 License

MIT